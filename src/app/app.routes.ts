import { Routes } from '@angular/router';
import { BattleScreenComponent } from './pages/battle-screen/battle-screen.component';

export const routes: Routes = [{ path: '', component: BattleScreenComponent }];
